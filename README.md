# multiclass-email-security-webapp
Cybersecurity web application for multiclass email classification using NLP and trained machine learning models.

# Model Artifacts

The trained model artifacts are not stored directly in this GitHub repository due to file size and deployment considerations.

The deployed model will be hosted through Hugging Face Spaces, where users can test the email classification system online.
